DATABASE_URL=postgres://postgres:password@localhost/poker_tracker

# Bcrypt hashing cost (default: 12)
# Lower values = faster but less secure. Use 4-6 for load testing, 12+ for production
# BCRYPT_COST=12

JWT_SECRET=your-secret-key-change-this-in-production
RUST_LOG=info
HOST=127.0.0.1
PORT=8080
