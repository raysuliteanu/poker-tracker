# Configuration can be provided via:
# 1. poker-tracker.toml (optional, see poker-tracker.toml.example)
# 2. Environment variables (override TOML values)
# 3. Hardcoded defaults

# Required values (no defaults):
DATABASE_URL=postgres://postgres:password@localhost/poker_tracker
SECURITY_JWTSECRET=your-secret-key-change-this-in-production

# Optional values (have defaults):
# SERVER_HOST=127.0.0.1
# SERVER_PORT=8080
# DATABASE_MAXCONNECTIONS=100
# DATABASE_MINIDLE=10
# SECURITY_BCRYPTCOST=12

RUST_LOG=info
